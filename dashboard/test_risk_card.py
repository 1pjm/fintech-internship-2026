"""
전세사기예측_AUC 비교.ipynb 실행 결과에 실제로 출력된 매물 1건
(X_tr.iloc[0], 대위변제여부 학습 데이터의 17310번 인덱스)의 SHAP 값을 그대로 가져와서
build_risk_card()가 올바르게 동작하는지 검증한다.

주의: prob(대위변제 확률)은 그 노트북이 X_te에 대해서만 출력해서 이 매물(X_tr 소속)의
실제 확률값은 알 수 없다. 여기서는 파이프라인 동작 검증을 위해 임의로 지정했다.
실 서비스에서는 model.predict_proba(row)[:, 1] 결과를 그대로 넣으면 된다.
"""
import pandas as pd

from risk_card import build_risk_card

# 노트북에 실제로 출력된 SHAP 값 (그대로 옮김)
shap_row = pd.Series({
    "보증완료월": 0.043,
    "주택가액": 0.048,
    "임대보증금액": -0.112,
    "선순위채권금액": -1.118,
    "시도": 0.218,
    "주택구분": -0.026,
    "전세가율": -0.327,
    "주택유형위험도": -0.010,
    "시세_평균전세보증금": 0.065,
    "시세대비보증금비율": -0.347,
    "월세전환비율": -0.038,
    "평균보증금인상률": 0.019,
    "갱신계약비율": -0.016,
    "갱신요구권행사비율": -0.015,
    "평균건축연도": -0.110,
    "평균전용면적": 0.056,
    "거래량": 0.038,
    "깡통지수": -0.774,
    "보증완료연도": -0.033,
})

# 참고용 임의 확률(위 주석 참고). 실서비스에서는 model.predict_proba 결과 사용.
card = build_risk_card(prob=0.12, shap_row=shap_row)

import json
print(json.dumps(card, ensure_ascii=False, indent=2))
